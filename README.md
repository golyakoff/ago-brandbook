# AGO Brandbook

AGO's own public brand identity reference — colors, type, and the real channel icons — served at its
own subdomain, separate from `ago-landing` (the marketing page) and `ago-console`/`ago-widget`
(the product), the same "each deployable surface gets its own repo" reasoning `ago-landing`'s own
README states for itself (`ago-root/docs/architecture/repositories.md`).

Filed as `docs/backlog/25-180-*.md` in `ago-root`. This README is a placeholder to give the repository
a `main` branch to build on; the real content, Dockerfile, and CI have not been written yet.
